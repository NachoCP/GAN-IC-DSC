# Tensorflow implementations of two different GANs

Requirements for running:
	
* python >=3.5.3
* tensorflow >=1.0.0
* scipy
* numpy
* matplotlib

This repository contains two GANs of different elements:

* 1D data
* mnist data